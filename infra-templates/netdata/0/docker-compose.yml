netdata:
  labels:
    io.rancher.scheduler.global: 'true'
  image: titpetric/netdata
  ports:
    - ${NETDATA_PORT}:19999
  cap_add:
  - SYS_PTRACE
  volumes:
    - /proc:/host/proc:ro
    - /sys:/host/sys:ro
    - /var/run/docker.sock:/var/run/docker.sock:ro
